Please run npm install and then npm start.

Both React and Json web server will start in 3000 and 3001 port used the Concurrently module to run at a same time.

Lazy loading concept has been added for the product component